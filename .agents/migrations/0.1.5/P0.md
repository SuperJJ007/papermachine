# P0 — PaperMachine replant baseline

Date: 2026-09-09. Status: passed. Product repository: SuperJJ007/papermachine.

## Inputs and scope

The approved local plan is `.agents/tmp/agent-work/2026-09-09-upstream-0.1.5-migration/PLAN.md` in the source checkout. Its SHA-256 is `ba2add1e764fd6a1aaabe3577981f273a679417c6c4b90f7e0631d6f54322c28`. The source main is `44575f3bf0`; the replant baseline is `dsh-v0.1.5-alpha.1`, commit `5dda764ed3aa172535a7967b06ff95d9cbfe536a`. The independent checkout is `/Users/superjj/ccproj/pm-replant`, branch `replant/0.1.5`.

During migration, new main changes to ui-conversation, ui-layout, ui-sidebar, apiproxy, and client-runtime are frozen under P0. Main fixes permitted by the plan must be inventoried for reimplementation on the new tree. This is a coordination rule, not a GitHub branch-protection setting.

## Baseline evidence

Acceptance checks ran against the unchanged upstream checkout on macOS arm64 with Node 24.14.0, pnpm 11.7.0, and Python 3.12.9. Dependency installation initially used Node 22.14.0 and emitted an engines warning; acceptance commands used the supported Node 24 runtime. Its tracked working tree is clean after verification.

| Command | Result |
| --- | --- |
| `pnpm install --frozen-lockfile` | Passed; lockfile unchanged |
| `pnpm run typecheck` | Passed |
| `pnpm run test:docs` | 16 passed, zero failed or skipped |
| `pnpm run build:native-system` | Built darwin-arm64 system.node |
| `pnpm run build` | Passed; 234 client artifacts recorded |
| `pnpm run test:snapshot` | 127 passed, 2 skipped; all 4 files passed |

The first snapshot attempt lacked the native addon. The initial Node 24 installation lacked development headers, so the same Node binary and official matching headers were assembled under `/private/tmp/pm-node-v24.14.0`. The macOS system Python was 3.9.6; the accepted run places `/opt/miniconda3/bin` on PATH. With native code available but only the typecheck build completed, all 18 SDK cases failed with `cannot create effect on inactive context`; the complete repository build resolved the failures. Future baseline replay must include the complete build prerequisite used by the upstream CI snapshot lane. No source or expected output was changed to make these checks pass.

## Legacy inputs and cleanup

One existing compressed session from each of Biology, SocialScience, and BusinessAnalytics was read and confirmed to have header version 0. Paths and SHA-256 digests are retained in local execution evidence; session contents are not committed. P3 must verify rejection and unchanged bytes. The user-report ZIP mentioned in the plan is not yet located; local V0 samples are available for that verification.

The source checkout's empty `C:\pm\pkgs` directory and `docs/.DS_Store` were removed. No tracked source changes resulted.

## Next stage

P1 owns the eight package manifests and compiler references, the science-app bundle and science profile, a runnable PaperMachine brand page, and the host/client stub ownership inventory. P1 is not complete until its installation, compiler, configuration, dependency, and browser acceptance checks pass. P2–P7 remain pending. The old main and user session files remain unchanged.
