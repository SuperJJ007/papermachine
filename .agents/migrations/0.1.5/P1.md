# P1 — Package import and product composition

Status: accepted for package import and branded composition. Science behavior remains owned by P2–P7.

## Imported source

The eight owned packages are imported from pre-replant main `44575f3bf0`, including their tests. Their versions are `0.1.5-alpha.1`. Client manifests and compiler references use the new store, Session Controller, and slots packages. The disabled ui-science package is temporarily excluded from the Client aggregate reference and test glob, as approved for P1 closeout. P4 starts by restoring both. Host source and tests remain included. Mechanical source updates cover ToolCallId, the values utility owner, and Session.snapshotEvents().

The science-app bundle and science profile are present. The Web bundle itself is unchanged. The CLI resolves the product bundle as a dependency; its profile selects base, web-app, then science-app. The bundle supplies its preset root through an explicitly injected Host value. The brand plugin uses the PaperMachine build guard and leaves document-title ownership to DSH_CLIENT_TITLE.

## Temporary entry ownership

| Entry | Owner | Current behavior and exit condition |
| --- | --- | --- |
| science-app/pending | P2 | Rejects agent/pre-step; replace with the restricted Science preset after Host acceptance |
| session-attachment-index | P2.5 | Disabled; adapt Text attachments to the File API |
| science-session | P2.2 and P3 | Disabled; adapt projection admission and V3 event/read-write behavior |
| science-artifact-store | P2 | Disabled pending assembled Host acceptance |
| science-runtime/with-settings | P2 | Disabled; finish subprocess, persistence, settings, and execution checks |
| tool-science/edit-service | P2.7 | Disabled; adapt and assemble the Remote/fetch API |
| ui-science | P4 | Disabled; replace old runtime/Detail integration with public sidebar and controller APIs |

The ui-science compiler deferral avoids disposable adaptations of APIs that P4 replaces. All other aggregate compiler inputs must pass in P1.

## Verified at acceptance

- `pnpm install --frozen-lockfile` passes under Node 24.14.0 and pnpm 11.7.0.
- `pnpm run typecheck` passes end to end, including Host compilation, tsdown/Typert generation, and Client compilation.
- `DSH_CLIENT_BUILD_PROFILE=papermachine DSH_CLIENT_TITLE=PaperMachine pnpm run build` passes and emits 238 client artifacts.
- `gen-tsconfig-paths.ts --check`, `verify-cordis-config.ts`, `verify-package-dependencies.ts`, and `gen-client-catalog.ts --check` pass.
- Focused subprocess execution and brand tests pass: 22 tests in 2 files.
- The source CLI starts with `--profile science` in an isolated temporary Harness home. The browser title and sidebar show PaperMachine; the hero shows 探索未至之境. Screenshot: the approved plan's `execution/P1/brand-startup.png`.

## Source stub handoff

Original function bodies are retained in import commit `7c1bc3df52`. These placeholders throw instead of constructing incomplete runtime values. Private helpers reachable only from a replaced body are removed with that body; the original commit remains the source for the owning phase.

| File under packages/ | Entry | Owner |
| --- | --- | --- |
| science/science-session/src/index.ts | apply | P2 checkpoint admission |
| science/science-runtime/src/environment.ts | observeProfile | P2 Host-local provider and interpreter observation |
| science/science-runtime/src/install.ts | runMicromambaInstall | P2 isolated installer environment |
| science/science-runtime/src/kernel-process.ts | KernelProcess.start, interrupt | P2 persistent subprocess and cooperative interruption |
| science/science-runtime/src/kernel-transport.ts | startResponseReader, createResponseFifo | P2 isolated FIFO subprocesses |
| science/science-runtime/src/index.ts | assertHostLocal | P2 execution-world verification |
| science/tool-science/src/context.ts | isScienceSession | P2 agentPreset projection |
| science/tool-science/src/edit-message.ts | submit | P2 verbatim raster admission |
| science/tool-science/src/edit-message.ts | addArtifactNote, removeArtifactNote | P3 required note events |
| client/ui-science/src/client/index.ts | apply | P4 public sidebar and Session Controller composition |

Attachment reference declarations use FileAttachmentRef. File carrier classification and UTF-8/media policy remain P2 work. Reconciliation uses list/open/read/close; a read handle closes in a finally block. Client imports for existing APIs point at their current owning packages; remaining presentation and Remote adaptations are not complete.

- `verify-translation-pairing.ts` and `verify-package-readme-limitations.ts` pass. The Cordis catalog is regenerated after the successful aggregate typecheck.

## Deferred acceptance

P2 owns real subprocess environments, interruption, output validity, and execution-world behavior. P1 fixtures omit unavailable fork fields with P2.1 markers; subprocess PID remains provider-private. P4 owns the excluded ui-science source and tests, including public slots, controller integration, Remote readers, and locale completion.

P6 owns the remaining documentation audit failures: imported README sections, tool/config catalog coverage, the Science group README, and links to unimported Agent Notes. The graph check reaches service-role classification and reports four missing Science roles; the earlier attachment-index unknown-symbol error is gone. This classification remains P6 work. Full doc-sync success is not claimed. P1 fixes only its catalog-generation, pairing, and new-package limitations side effects. P2–P7 behavior acceptance remains outstanding.
