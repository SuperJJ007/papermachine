# P1 — Package import and product composition

Status: in progress. This checkpoint is not P1 acceptance.

## Imported source

The eight owned packages are imported from pre-replant main `44575f3bf0`, including their tests. Their versions are `0.1.5-alpha.1`. Client manifests and compiler references use the new store, Session Controller, and slots packages. No package or test is excluded from compilation to hide an obsolete API. Mechanical source updates cover ToolCallId, the values utility owner, and Session.snapshotEvents().

The science-app bundle and science profile are present. The Web bundle itself is unchanged. The CLI resolves the product bundle as a dependency; its profile selects base, web-app, then science-app. The bundle supplies its preset root through an explicitly injected Host value. The brand plugin uses the PaperMachine build guard and leaves document-title ownership to DSH_CLIENT_TITLE.

## Temporary entry ownership

| Entry | Owner | Current behavior and exit condition |
| --- | --- | --- |
| science-app/pending | P2 | Rejects agent/pre-step; replace with the restricted Science preset after Host acceptance |
| session-attachment-index | P2.5 | Disabled; adapt Text attachments to the File API |
| science-session | P2.2 and P3 | Disabled; adapt projection admission and V3 event/read-write behavior |
| science-artifact-store | P2 | Disabled pending assembled Host acceptance |
| science-runtime/with-settings | P2 | Disabled; finish subprocess, persistence, settings, and execution checks |
| tool-science/edit-service | P2.7 | Disabled; adapt and assemble the Remote/fetch API |
| ui-science | P4 | Disabled; replace old runtime/Detail integration with public sidebar and controller APIs |

These configuration entries do not excuse compiler errors in their imported source. P1 requires those errors to be resolved before acceptance.

## Verified at this checkpoint

- Dependency installation succeeds under Node 24.14.0 and pnpm 11.7.0. The complete build is still needed to materialize CLI bin links.
- verify-tsconfig-paths passes.
- verify-cordis-config passes for 143 configuration files.
- verify-package-dependencies passes for 62 policy-managed packages.
- The real CLI exports the science profile through --profile science --dump-config in an isolated temporary Harness home.
- Focused brand and byte-size tests pass: 7 tests in 2 files. Brand tests cover the build-profile guard, deferred declarations, and plugin disposal.
- Persistence and session-format catalog generators run. The dependency fixer regenerates the bilingual module graph.

## Remaining P1 acceptance

Host compilation fails on subprocess extensions, checkpoint options, persistence handles, attachment APIs, and V3 test fixtures. Client compilation has obsolete runtime imports and Detail slots; its preliminary diagnostics also include absent generated Remote declarations because the Host build has not completed. The client catalog refuses nine registrations into undeclared old slots. The Cordis catalog cannot resolve the old TextAttachmentRef type. These are migration failures, not an accepted baseline or environment exception.

The complete typecheck, remaining catalog generation, doc-sync, compiled PaperMachine browser startup, and final per-file stub inventory remain pending. No browser screenshot or working Science execution is claimed. P2–P7 have not started. Local command logs remain beside the original approved plan under execution/P1.
