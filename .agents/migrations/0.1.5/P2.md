# P2 — Core adaptation

Status: P2 closeout implemented and checks executed; not an unconditional all-green result. P4/P6 failures, including three additional ui-theme repository-wide CSS assertions, are recorded explicitly below. P1 acceptance is recorded at `ce76a86204`; original P2 ended at `1089ef24a1`.

## Implemented scope

| Plan item | Result |
| --- | --- |
| P2.1 subprocess and confinement | Explicit empty versus scrubbed target environments, cooperative interruption through provider-private process owners, execution-world admission, incremental UTF-8 validity, sandbox classification and required environment overrides, terminal propagation, and Windows ACL console initialization. |
| P2.2 projections and owned stubs | Current Session APIs and branded ids; checkpoint state validation and watermark consistency share cold-view, restore, and hydration admission. The temporary Science refusal plugin is removed. |
| P2.3 preset identity | Science eligibility uses the upstream agentPreset projection definition. |
| P2.4 prompt order | Science guidance uses order 1100. |
| P2.5 attachments | Durable File references, V3 reference scanning, bounded UTF-8 text previews, and explicit verbatim image admission. |
| P2.6 preset policy | Read-only filesystem tools, MCP tool restrictions, authoritative copyable policy and client presentation, effective settings and secret metadata, compaction exemptions, and runtime context restoration after retry-time surface replacement. |
| P2.7 reads and download | Five generated Science Remotes, API assembly, authenticated exact GET/HEAD artifact route, server-generated versioned download names, and RemoteResult/RemoteError type migration. HTTP registration is optional so the same read service loads without a Web connection. |
| P2.8 enforcement | minimumEnforcement remains configurable; the Windows partial-enforcement deployment choice remains with P5. |
| P2.9 headless | science-headless composes the Science Host services and restricted preset. The direct driver mounts the configured preset before its first request; the Science overlay disables base model-facing tools. |

## Acceptance evidence

- Complete Science unit run: 55 passed files, 3 conditionally skipped; 1,024 passed tests, 34 conditionally skipped. Statements, branches, functions, and lines are each 100%. The final download-name follow-up passes all 14 read-service tests and retains 100% for that changed file.
- [Real Conda acceptance](evidence/P2/real-acceptance.json): Python 32/32, R 27/27, coexistence 4/4. The entry script and kernel assets run from a Git archive of candidate `847328fb1a0aa54275c71705ba6ee73703f8856c`, using the installed workspace dependency tree. Both interpreters use the local `general/2026.09.1` prefix and an isolated private test home. Prefix-manifest checks pass.
- [Real headless artifact evidence](evidence/P2/headless-artifact.json): one run_python call at sequence 20, successful science/run-finished at 23, and science/artifact-saved at 24. The persisted PNG has SHA-256 `505b90894c0fc294557ed8f19ed940936a811a15488d9f4fa8954db49390050b`. The compressed V3 log was read across all Zstandard frames to verify these facts.
- Host aggregate compilation and Host bundle/Remote generation pass. Client aggregate compilation passes. The plain-Node read-only filesystem package smoke passes.
- Adjacent Host package run: 2,342 passing tests, with seven probe/timeout failures in two files during concurrent validation. A focused rerun of those two files plus E2B and Science read tests passes all 143 tests. The two earlier failures are not represented as an all-green first run.
- Settings and preset Client-adjacent run: 775 passing tests and one outdated exact-snapshot expectation; after correction, its owning file passes all 25 tests.
- Dependency policy, Cordis configuration, type-equivalent documentation, Agent Note format, and translation pairing checks pass. Normal staged lint, whitespace, notices, and vendor hooks run on every commit.

Windows evidence from P2.1 remains valid: on pmwin with Node 24.19.0 and Koffi 3.1.1, the detached upstream ACL runner fails with 0xC0000142; console initialization makes attached and detached primitive probes and actual runner launches exit zero with piped stdout. This does not claim packaged Electron or Windows Science kernel acceptance.

## Remaining phases

P3 owns the historical Session format and complete Science event migration. P4 owns the disabled ui-science integration and browser acceptance. P5 owns packaged desktop acceptance. P6 owns the imported documentation hierarchy, generated catalogs, links, and rebuilt Science snapshots. The full doc-sync run is not green: its imported catalog, graph, reference, hierarchy, and README gates remain recorded in the approved execution/P2 logs. P2-introduced type-equivalence, test typing, and Agent Note format failures were corrected and checked separately.

The approved plan's execution/P2 directory retains command logs and native Windows evidence. No push, merge, or replacement of main was performed.

## Closeout

Closeout starts at `1089ef24a182cd4253b0092f6bf99d3023f7f28a` and ends at the commit containing this section (W10). All work is on `replant/0.1.5` in `/Users/superjj/ccproj/pm-replant`; no push, main update, or DSHscience build was performed. W4/W5 followed the code changes so the ledger could measure them. W7 and W9 each have a focused acceptance-driven follow-up: the old publication list omitted required runtime files, and temporary directories require canonical-path comparison on macOS.

### Commits

- `4c6f64d09b` — fix(catalog): 登记 Science Remote 签名类型归属（W1）
- `81d5a150c4` — fix(p2): 修复移栽新增的六处 lint 错误（W2）
- `abd84364ae` — refactor(sandbox): Bash 与 Pwsh 共用公共分类器（W3）
- `1d767616d8` — fix(science): 按项目目录授权产物读取（W6）
- `2f2a6fbc15` — fix(hygiene): 对齐发布清单并登记内核测试驱动（W7）
- `ba6552fa4d` — fix(p2): 整理 fixture 并完善真实验收诊断（W9）
- `bb36e06d0c` — refactor(science): 工作区文件读取交给上游 Remote（W8）
- `96914ad8aa` — docs(replant): 补齐上游补丁台账与 D9–D11 决定（W4）
- `e5ff4e1e6f` — docs(replant): 引用待提交的上游回馈清单（W5）
- `be7941fc25` — test(science): 保存 headless 真任务与 blob 校验（W11）
- `90e99febc9` — fix(hygiene): 将必需发布载荷登记到精确清单（W7）
- `6528088e9f` — fix(science): 真实验收拒绝临时目录的规范路径别名（W9）
- W10 — this record, final ledger accounting, and retained acceptance evidence.

### Acceptance table

All commands run in pm-replant unless a package cwd is stated. Node 24.14.0 comes from `/private/tmp/pm-node-v24.14.0/bin`; Python 3.12 comes from `/opt/miniconda3/bin`. Build uses `npm_config_nodedir=/private/tmp/pm-node-v24.14.0`. [Machine-readable results](evidence/P2/closeout-checks.json) retain exits and per-package coverage.

| Check | Actual result |
| --- | --- |
| `pnpm install --frozen-lockfile` | PASS, exit 0; already up to date, no lockfile change. |
| `pnpm run typecheck` | PASS, exit 0; Host build/Remote generation and Client aggregate complete. |
| `pnpm exec tsx scripts/gen-tsconfig-paths.ts --check` | PASS, exit 0. |
| `pnpm exec tsx scripts/verify-cordis-config.ts` | PASS, exit 0. |
| `pnpm exec tsx scripts/verify-package-dependencies.ts` | PASS, exit 0; both shell packages already declare sandbox peer+dev dependencies. |
| `pnpm exec tsx scripts/gen-client-catalog.ts --check` | PASS, exit 0. |
| `pnpm exec tsx scripts/gen-cordis-catalog.ts --check` | PASS, exit 0; signature ownership and scienceReads page registration are complete. |
| `pnpm exec tsx scripts/verify-agent-note-format.ts` | PASS, exit 0; 313 Notes. |
| `pnpm exec tsx scripts/verify-type-equiv.ts` | PASS, exit 0; 413 blocks and 413 paired derivatives. |
| `pnpm exec tsx scripts/verify-translation-pairing.ts` | PASS, exit 0; 783 pairs. |
| `pnpm exec tsx scripts/verify-export-jsdoc.ts` | PASS, exit 0. |
| `pnpm run lint` | FAIL, exit 1: exactly 480 errors, all under `packages/client/ui-science` (P4). P2’s six errors are fixed. |
| `pnpm run duplication` | FAIL, exit 1: one ui-science library-artifact ↔ tool-science read-types clone (P4); shell classifier clones are gone. [Log](evidence/P2/duplication-closeout.log). |
| `pnpm run hygiene` | FAIL, exit 1: 13 passed, only package invariants (P6), client packages (P4), client UI i18n (P4) fail. An earlier run also failed publint after applying the brief’s incomplete files lists; the named payload correction removes that failure. [Final log](evidence/P2/hygiene-closeout.log). |
| `pnpm exec vitest run --coverage packages/science --coverage.include='packages/science/*/src/**/*.ts'` | PASS, exit 0: 55 files / 1024 tests passed, 3 files / 34 tests conditionally skipped. All four Science packages are 100% statements, branches, functions, and lines. Explicit coverage scope measures all four packages without applying unrelated packages’ thresholds. |
| Modified-package suites | FAIL, exit 1: 57 files / 781 tests pass, 2 files / 3 tests fail, 3 files / 25 tests skip. All failures are ui-theme repository-wide CSS assertions against unchanged ui-science styles (P4). Tool-science and science-runtime are covered by the full Science run above. [Log](evidence/P2/adjacent-tests-closeout.log). |
| `pnpm run test:real-acceptance` in `packages/science/science-runtime` | PASS, exit 0: Python 32/32, R 27/27, coexistence 4/4. [JSON](evidence/P2/real-acceptance-closeout.json). |
| `pnpm run build` | PASS, exit 0 with the specified Node-API headers. |
| W11 science-headless real task | PASS, exit 0: run_python seq 20, successful run-finished seq 23, artifact-saved seq 24. PNG blob is 54,232 bytes; SHA-256 matches the event. [JSON](evidence/P2/headless-closeout.json). |
| `pnpm run doc-sync` | FAIL, exit 1: 25 passed, 9 failed (P6): doc graphs, Markdown links, tool catalog, config catalog, doc refs, subsystem pages, README Summaries, README model experience, documentation standard tests. [Log](evidence/P2/doc-sync-closeout.log). |
| Extra focused validation | PASS: 15 workspace-constraint tests, the plain-Node tool-fs built smoke, archived-note verifier, application-entrypoint/archived-note tests in the adjacent run, and focused lint for changed scripts. |
| `git status --short` | Empty after this commit; no test-created project marker remains in the worktree. |

The adjacent command is `pnpm exec vitest run packages/sandbox/sandbox/ packages/sandbox/sandbox-local/ packages/shell/bash-sandbox/ packages/shell/pwsh-sandbox/ packages/session/session-projection/ packages/compaction/compaction-tool-result-pruner/ packages/subprocess/subprocess-local/ packages/api/remotes/ packages/client/connection/ packages/client/ui-permission-presets/ packages/client/locale/ packages/client/ui-theme/ scripts/verify-application-entrypoints.spec.ts scripts/verify-archived-agent-notes.spec.ts`. Its three failures are `corner-shape-styles.client.spec.ts`’s round-corner pairing assertion (11 ui-science selectors) and `elevation-styles.client.spec.ts`’s hairline-border/divider assertions (45 border findings and 2 divider findings). No test expectations or P4 styles were weakened to obtain a green result. The reported ambient spawn-runner failure did not recur in this shell.

### Decisions and real-run provenance

D9 keeps required `effective`; the [patch ledger](../../notes/implemented/process/2026-09-09-replant-upstream-patch-ledger.md) records why and the full client-fixture list. D10 removes the two owned workspace-file Remotes; the read service retains a separately named `textAttachmentByteLimit` for referenced UTF-8 files. Upstream workspace-files and ui-sidebar-files remain enabled, and P4 owns replacing the remaining ui-science calls. D11 is implemented in read-service authorization and its owning bilingual Note: project reads require cwd, local artifacts use event coordinates, and any other same-project version is store-verified. Same-project cross-session and cross-project-denial tests cover the intended permission distinction.

Real acceptance uses candidate `e5ff4e1e6ffac24839433688c5110ca4e95ac97c`, `DSH_SCIENCE_RUNTIME_REAL_ACCEPTANCE=1`, `DSH_SCIENCE_RUNTIME_TEST_OWNED=1`, `DSH_SCIENCE_RUNTIME_DSH_HOME=/Users/superjj/ccproj/pm-p2-closeout-home/real-home`, and the same `DSH_HOME`. Both language prefixes point to `/Users/superjj/.papermachine/desktop-environments/environments/general/2026.09.1`; prefix manifests remain unchanged. The home is mode 0700. Later changes affect publication metadata, the real-acceptance home guard, and records, not kernel implementation. The final guard is also exercised through its actual entrypoint for `/tmp`, `/private/tmp`, and `os.tmpdir()` directories; all three are refused before kernel startup. These deliberately rejected negative probes report NOT-RUN in their [evidence](evidence/P2/temp-root-refusal-closeout.json).

Headless uses `DSH_HOME=/Users/superjj/ccproj/pm-p2-closeout-home/headless`, `--profile science-headless`, and `--patch /Users/superjj/ccproj/pm-p2-acceptance/runtime.patch.yml`. The 32-event V3 log is decompressed with `read_across_frames=True`; the recorded PNG hash equals `8e19073b995e8c13cf0f268e4c461f5a5ce262ffd6493a1ed00f814b79469f58`. The run-created `.papermachine/project.json` is retained as `headless/workspace-project-marker.json` under the isolated home and removed from the worktree. Neither `~/.dsh` nor `~/.papermachine` is written.

NOT-RUN acceptance items: none. Conditional test skips remain listed above; Windows packaged acceptance belongs to P5 and is not claimed here. The three ui-theme CSS failures prevent describing the requested modified-package row as all-green; Opus should carry them with P4’s existing UI work.
