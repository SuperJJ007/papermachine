# P3 — Session format

Date: 2026-09-10, Asia/Taipei. Status: **P3 accepted**. The repository is **not ready to merge into main**: the P4/P6 failures below remain. Base: `37b324ab70437e54a2d363c6f6d343ef625dd4af`, branch `replant/0.1.5`, checkout `/Users/superjj/ccproj/pm-replant`. Acceptance ran against the working tree committed with this record; the final commit id is reported at handoff.

## Implemented scope

All nine Science events remain in SessionEventMap and are required on read. The obsolete IgnorableSessionEventMap augmentation is removed; Science replay now also rejects ignorable note-add and note-remove envelopes. The note Remotes already used normal append in the P2 base, so P3 verifies those writers rather than reimplementing them. Notes remain independent durable UI state, without model messages or an agent follow-up. The seven model-state events retain their existing fold and preset applicability rules.

The persistence catalog and its Chinese declaration copies are regenerated/synchronized. The current known-event set already contained all nine types and regenerates without a source diff. The historical codecs, inventories, and migration edges are unchanged. The only added package dependency is the JSONL provider as a development dependency of the owning storage regression suite.

The bilingual [release-note draft](../../../docs/user/papermachine-0.1.3-release-notes.md) explicitly states that PaperMachine 0.1.0 Science sessions cannot be opened and original files remain intact. The [Agent Note](../../notes/implemented/architecture/2026-09-10-science-required-session-events.md) records D1/D7 and the deferred recovery work: dispositions, Science sequence-reference remapping across all three edges, and real-log replay validation; estimate two to three days, without a delivery commitment. An active-note supersession search found no earlier Science-specific ignorable/V0 decision to archive; the generic released-format decisions remain active.

## Behavioral acceptance

- **333 tests pass in 19 files** across science-session and tool-science; both changed packages reach **100% statements, branches, functions, and lines**. The new storage suite has 38 tests: two native V3 round trips plus all nine legacy types with both required/ignorable envelopes in both plain and Zstandard storage. Every legacy case attempts read and write open, checks the precise historical-type refusal, preserves bytes and inode, and observes no successor. Native V3 checks all nine events, active-note restoration, removal by add-event sequence, and unchanged Science projections and sequence references.
- **1,269 tests pass in 32 files** across session-format-catalog, session-persistence-jsonl, and all three adjacent migration packages. Generic historical migration support is retained; no blanket V0 ban or Science opaque disposition is introduced.
- **Complete test:snapshot passes: 127 passed, two conditional skips, all four files passed.** The first complete run exposed two pre-existing P2 expectation gaps: compaction-recovery lacked retry-time runtime context, and cordis-inspect-jsdoc lacked the new subprocess executionWorld/interrupt/utf8Validity/environmentBase declarations. Only those two current V3 fixtures were refreshed to match verified P2 behavior, then the complete lane passed. No assertion, normalizer, or historical V2 generation was changed.
- **TypeScript SDK refresh: 18/18 pass.** Only multi-turn writer.expected.jsonl changes, aligning embedded stream timestamps with the selected fixture; no model text or protocol event is added or removed by this SDK refresh.
- **Python SDK: all four expected-output groups refresh and replay successfully** with the newly built macOS arm64 single executable: sdk-snapshot (advanced), sdk-restart, sdk-minimal, and sdk-minimal-in-history. Expected files remain byte-identical to HEAD, which is a verified generation fixed point rather than a skipped refresh. [Commands](evidence/P3/python-refresh-checks.json), [replay commands](evidence/P3/python-replay-checks.json), and [artifact/expected-file hashes](evidence/P3/python-artifact.json) are retained. This is explicit-executable source-SDK acceptance, not an installed-wheel or cross-platform release claim.

## Real V0 evidence

The Biology, SocialScience, and BusinessAnalytics compressed logs inventoried by P0 are each read-only inputs. Their SHA-256 values still match P0. Built JSONL persistence opens isolated byte-identical copies in matching project-directory layouts, once for read and once for write. All six attempts return SessionFormatUnsupportedError naming science/mode-bound as an unknown historical type. Original and copy bytes remain unchanged; original mtime and inode remain unchanged; no versioned successor appears. [Evidence](evidence/P3/real-v0.json) contains digests and diagnostics without private session contents. The unlocated user-report ZIP is not needed for this acceptance: three actual released V0 samples were checked.

## Commands and results

All target commands run in pm-replant. PATH begins with `/private/tmp/pm-node-v24.14.0/bin:/opt/miniconda3/bin`; the build also sets `npm_config_nodedir=/private/tmp/pm-node-v24.14.0`. Node is 24.14.0, pnpm 11.7.0, Python 3.12.9; the packaged Node24 executable selects embedded Node 24.21.0. [Machine-readable commands and exits](evidence/P3/checks.json) retain the actual attempts and logs. Work helpers used during execution are copied into [evidence/P3/helpers](evidence/P3/helpers/verify-real-v0.mjs).

| Command | Result |
| --- | --- |
| pnpm install --lockfile-only; final CI=true pnpm install --frozen-lockfile --prod=false | PASS; final lockfile differs only by the JSONL test dependency. |
| pnpm run gen-persistence-catalog | PASS; generated note JSDoc reflects required-on-read semantics. |
| pnpm run build | PASS; Host and Client compilation/bundling and 238 client artifacts. |
| Focused coverage and adjacent migration suites | PASS, counts above; full argv in checks.json. |
| pnpm run test:snapshot:refresh snapshots/sdk/sdk.snapshot.ts | PASS, 18 SDK cases. |
| pnpm run test:snapshot | PASS on final fixtures, 127 passed / two skipped. |
| Python executable build and four refresh/replay scenarios | PASS with command-scoped packaging options described below. |
| gen-persistence-catalog.ts --check; gen-session-format-catalog.ts --check | PASS; current catalogs are fresh. |
| verify-package-dependencies.ts | PASS. |
| Focused run-oxlint.ts on all five changed TypeScript files | PASS. |
| pnpm run lint:contracts-ready after the complete build | FAIL: 480 errors, all in untouched ui-science (P4). |
| pnpm run doc-sync | FAIL: 25 passed / nine failed, the same P6 gate families recorded by P2. Translation pairing, note format, export JSDoc, type equivalence, and build checks pass. |
| pnpm run test:docs | FAIL: 11 passed / five failed, a subset of the P6 documentation debt. |
| git diff --check | PASS. Normal pre-commit hooks run at commit; the initial whitespace refusal is recorded below. |

## Packaging and validation limitations

The unmodified Python build command fails in pnpm 11 legacy deploy with ERR_PNPM_UNUSED_PATCH for the Electron-only app-builder-lib patch. A first environment-variable attempt does not apply that pnpm setting. The retained build wrapper passes `--config.allowUnusedPatches=true` only to deploy; it does not remove or suppress application of any used patch. Deploy succeeds, but the next pnpm exec auto-check can request a production-only root install and lose development tools (lefthook/tsx). The final wrapper also passes `--config.verifyDepsBeforeRun=false`; the complete frozen development installation is explicitly restored before and after packaging. Neither option is committed to pnpm-workspace.yaml or the product builder. The ordinary unwrapped Python build remains a tooling follow-up for P6; the actual current executable and Python projections have passing evidence.

Initial synthetic-test failures were fixture setup errors (V0 header fields, Zstandard header-frame separation, and the read-handle header location), corrected before acceptance. The first real-copy probe also used a project-directory path inconsistent with its header; correcting the isolated copy layout produced the required semantic refusal. No original file was written. Intermediate documentation attempts include new-pair omissions and the transient missing-dependency state; the final closeout logs supersede those attempts and show only the known P6 failures.

The initial commit hook rejects trailing whitespace emitted by build/test tools in the retained logs. Checked-in text logs remove trailing line whitespace and surplus blank lines at EOF; [raw-logs.tar.gz](evidence/P3/raw-logs.tar.gz) retains the exact original transcripts, including the hook refusal. No hook is skipped.

## Remaining work and scope

No P3 behavioral acceptance item is blocked. P4 still owns 480 ui-science lint errors, the client duplicate/import/i18n issues, and the three ui-theme CSS assertions described in the P2 independent acceptance. P6 still owns doc graphs, Markdown links, tool/config catalogs, doc refs, subsystem pages, README Summaries/Model Experience, and documentation standard failures. The ordinary Python packaging command issues above are additionally disclosed for P6. Duplication, hygiene, and ui-theme CSS were not rerun for P3; their P2 failures are retained as unresolved, not relabelled passing.

No upstream runtime package is patched by P3; the extra upstream files are generated persistence documentation and the three reviewed snapshot outputs. No Windows/Electron packaging, installed-wheel matrix, real-provider API, full-repository coverage, or P4 browser acceptance is claimed. No installed ~/.papermachine or ~/.dsh data, DSHscience product code, remote branch, push, or main merge is changed. Package-manager and executable-build caches receive ordinary build writes. Historical Science recovery, P4/P5 features, and P6's broader Science snapshot reconstruction remain deferred.
