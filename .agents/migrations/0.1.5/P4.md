# P4 — Science 客户端迁移验收

日期：2026-09-10，Asia/Taipei。状态：**P4 已验收**。工作树 `/Users/superjj/ccproj/pm-replant`，分支 `replant/0.1.5`，本次收尾基于 `2be1c88b42`。本记录与实现一起提交；提交号在任务交接中报告。本次范围仅为 P4。

## 完成内容

Science 产物、Library、provenance 和 chart 使用原生右侧栏；Process 与 Trajectory 使用平级入口。空白会话保留原生右上角展开按钮，Guide 可打开产物库和文件。左侧 Library 入口已删除，没有新增私有导航槽位。Library 使用 Fluent Color 图标并包含许可声明；Guide 描述、库标题图标和原生控件的浏览器预期输出同步更新。

右侧栏通过 store persistence 保存每会话当前布局和身份计数，恢复时验证布局节点、分栏比例、浮窗、活动页签、引用关系和计数。畸形数据原子拒绝，由 store 保留可用初始状态。撤销历史仅属于当前页面，不写入浏览器存储，刷新后重新开始；有效布局恢复后继续生成不冲突的身份。README、原生侧栏 Agent Note 和上游补丁记录已同步中英文及配对记录。

## 验证

| 检查 | 结果 |
| --- | --- |
| Science、品牌、右侧栏、会话、布局、store 六组客户端测试 | 81 文件、1,221 项中首次 1,219 通过；两个旧 Library 入口/可访问名称断言修正后，其所属文件 5/5 复验通过。其余已通过文件未重复运行。 |
| 新持久化解析模块覆盖率 | 14 项测试通过；语句、分支、函数、行均为 100%。 |
| Science Web 八组 replay | 8 文件、28 项全部通过，无跳过。 |
| build、typecheck、lint | 通过；build 生成 238 个客户端产物。 |
| UI i18n、duplication、client catalog | 通过；655 个客户端源文件使用 locale 文案，零重复块，目录最新。 |
| ui-science publint、包依赖检查 | 通过；publint 提示现有 `./src/*` 导出未包含于发布文件。 |
| doc-sync | 25 项通过、9 项既有失败，未宣称全仓文档通过。 |

八组浏览器测试覆盖 stop、artifact types、trace process、trace cold restore、chart outcome、transcript chrome、file library 和 settings。测试保留真实冷会话数据验证；浏览器历史观察可能在首帧前激活会话，因此不再要求 UI 捕捉瞬时 started 状态，仍验证冷快照中的 started 和激活后的 interrupted。多产物测试通过稳定的会话查找打开输入；不提前激活会话，也不盲选首行。所有预期输出变更均对照原生 UI 行为审查，没有放宽 normalizer。

[逐项命令、结果和日志 SHA-256](evidence/P4/checks.json) 与 [原始验证日志](evidence/P4/validation-logs.tar.gz) 保留在仓库。测试使用 Node 24.14.0；构建显式选择本机可用的 MacOSX15.2 SDK，默认 MacOSX27 SDK 与当前 linker 不匹配。浏览器回归使用默认 DSH Local Build 配置，品牌插件由客户端包测试覆盖，不把这些浏览器结果当作发行品牌构建的证明。

## 范围和限制

全仓文档的九个失败类别与既有验收一致：doc graphs、Markdown links、tool catalog、config catalog、doc refs、subsystem pages、README Summaries、README Model Experience、documentation standard tests。本次只记录，不处理其他阶段的文档迁移；P4 验收不表示整个迁移或发布已验收。没有执行 push、替换 main 或发布。
